namespace Soundwave.Api.Controllers;

public class PlaylistsController
{
    /*
     * POST /playlists — создать новый.
     * PUT /playlists/{id} — изменить (приватность/название).
     * POST /playlists/{id}/tracks/{trackId} — добавить трек в плейлист.
     */
}